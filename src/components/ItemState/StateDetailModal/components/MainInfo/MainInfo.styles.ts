import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  modalText: {
    marginBottom: 16,
    textAlign: 'left',
    fontSize: 16,
    flexWrap: 'wrap',
  },
  info: {
    alignItems: 'flex-start',
  },
});
